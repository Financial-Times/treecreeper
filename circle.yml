machine:
  node:
    version: 7.10.0
  services:
    - neo4j
deployment:
  production:
    branch: master
database:
  override:
    - node scripts/init.js
test:
  post:
    - mv coverage $CIRCLE_ARTIFACTS
